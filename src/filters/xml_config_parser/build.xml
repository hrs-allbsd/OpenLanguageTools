<?xml version="1.0"?>

<!-- this is a child build script -->

<project name="xml-config-parser" basedir="." default="all">
	<import file="../../../common.xml"/>
	
    <path id="build.classpath">
		<path location="${build.dir}"/>
	</path>

</project>
