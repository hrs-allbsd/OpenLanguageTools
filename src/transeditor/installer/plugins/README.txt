Put you plugins (*.jar) in this directory. Resource files used by
these plugins should go into the resources sub-directory.

You may also create a plugin directory in $HOME/.xliffeditor/plugins (UNIX)
or %UserProfile%\.xliffeditor\plugins (Windows)